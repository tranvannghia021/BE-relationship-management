#### Start docker compose and attach in background

```bash
docker-compose up -d
```

#### View service logs

```bash
docker-compose log -f <<service-name>>
```

#### Remove all running service

```bash
docker-compose down
```
